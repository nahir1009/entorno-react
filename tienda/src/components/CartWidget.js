import Badge from '@material-ui/core/Badge';

const CartWidget = () => {
    return(
        <Badge badgeContent={4} 
        color="primary">
          <MailIcon />
        </Badge>
    )
    

}